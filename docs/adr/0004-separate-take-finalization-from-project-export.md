# Separate Take finalization from Project export

Stopping a recording finalizes its ordered Segments into one in-app Take owned by the current Project; it no longer saves to Photos. A separate, explicit Project Export concatenates every Take in Timeline order and saves an independent movie to Photos, allowing the user to review, reorder, re-record, and delete Takes without making the Project depend on Photo Library assets.
