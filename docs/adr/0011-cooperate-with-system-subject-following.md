# Cooperate with system subject following

On supported front-camera configurations, Camenya enables Center Stage by default and exposes it as the secondary Capture option Follow Subject while using cooperative system control. The latest in-app or Control Center choice persists as an app-level preference and is never silently overridden, balancing automatic talking-head framing with user control without adding another permanent Capture button.
