# Keep Project media ephemeral and local-only

Project media is owned by the app, never synchronized, and explicitly excluded from device and cloud backups because recordings are intentionally short-lived. Projects do not expire automatically and are deleted only by an explicit user action. This trades recovery after device loss, app deletion, or storage failure for privacy and a deliberately ephemeral lifecycle; only a movie explicitly exported to Photos leaves that boundary.
